# -*- mode: python -*-
a = Analysis(['mdr24.py'],
             pathex=['C:\\Users\\Administrator\\Desktop\\Robot project\\adr-crawler'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='mdr24.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='mdr24')
